import Link from "next/link";
export const dynamic="force-dynamic";
const SITE=(process.env.PUBLIC_APP_URL||process.env.NEXT_PUBLIC_SITE_URL||process.env.RENDER_EXTERNAL_URL||"https://bharatshop-9w4a.onrender.com").replace(/\/$/,"");
type Props={searchParams:Promise<Record<string,string|string[]|undefined>>};
export default async function CheckoutSuccess({searchParams}:Props){const q=await searchParams,provider=String(q.provider||""),gatewayOrderId=String(q.gateway_order_id||"");let result:any={verified:false,error:"Payment verification details are missing."};if(provider==="cashfree"&&gatewayOrderId){try{const r=await fetch(`${SITE}/api/payments/cashfree/status?gateway_order_id=${encodeURIComponent(gatewayOrderId)}`,{cache:"no-store"});result=await r.json();if(!r.ok&&!result.error)result.error="Unable to verify payment.";}catch(e){result={verified:false,error:e instanceof Error?e.message:"Unable to verify payment."};}}return <main className="min-h-screen bg-slate-950 flex items-center justify-center p-4"><section className="w-full max-w-lg rounded-3xl bg-white p-7 text-center shadow-2xl">{result.verified?<><div className="mx-auto w-14 h-14 rounded-full bg-green-100 text-green-700 flex items-center justify-center text-3xl">✓</div><h1 className="text-3xl font-black mt-4">Confirmation received</h1><p className="text-slate-500 mt-2">Your online confirmation amount is verified. The remaining balance is payable on delivery after BharatShop’s CEO agents complete fulfilment checks.</p><div className="rounded-2xl bg-orange-50 p-4 mt-5"><p className="text-xs text-orange-700">Balance payable on delivery</p><b className="text-2xl">₹{Number(result.codBalanceInr||0).toLocaleString("en-IN")}</b></div>{Array.isArray(result.orderRefs)&&<div className="mt-4 space-y-2">{result.orderRefs.map((ref:string)=><p key={ref} className="font-mono text-sm font-bold bg-slate-50 p-2 rounded-lg">{ref}</p>)}</div>}</>:<><div className="mx-auto w-14 h-14 rounded-full bg-amber-100 text-amber-700 flex items-center justify-center text-3xl">!</div><h1 className="text-3xl font-black mt-4">Payment not confirmed yet</h1><p className="text-slate-500 mt-2">{result.error||`Current gateway status: ${result.orderStatus||"pending"}. Your order will not enter fulfilment until payment is verified.`}</p></>}<Link href="/store" className="mt-6 h-12 rounded-xl bg-slate-900 text-white font-black flex items-center justify-center">Return to BharatShop</Link></section></main>}
