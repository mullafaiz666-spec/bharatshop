import { NextResponse } from "next/server";
import { verifyCashfreeWebhook } from "@/lib/payments/gateway";
import { markGatewayPayment } from "@/lib/payments/order-state";
export const runtime="nodejs";export const dynamic="force-dynamic";
export async function POST(req:Request){try{const raw=await req.text(),timestamp=req.headers.get("x-webhook-timestamp")||"",signature=req.headers.get("x-webhook-signature")||"";if(!verifyCashfreeWebhook(raw,timestamp,signature))return NextResponse.json({error:"Invalid webhook signature"},{status:401});const event=JSON.parse(raw),type=String(event?.type||event?.event||""),providerOrderId=String(event?.data?.order?.order_id||event?.order?.order_id||""),payment=event?.data?.payment||{},paymentStatus=String(payment?.payment_status||"").toUpperCase();if(!providerOrderId)return NextResponse.json({received:true,matched:false});let status:"TOKEN_PAID"|"TOKEN_FAILED"|"TOKEN_REFUNDED"|null=null;if(paymentStatus==="SUCCESS"||/SUCCESS/.test(type))status="TOKEN_PAID";else if(paymentStatus==="FAILED"||/FAILED/.test(type))status="TOKEN_FAILED";else if(/REFUND/.test(type))status="TOKEN_REFUNDED";if(!status)return NextResponse.json({received:true,matched:false,event:type});const state=await markGatewayPayment({provider:"cashfree",providerOrderId,status,event:type||paymentStatus,paymentId:String(payment?.cf_payment_id||"")||undefined});return NextResponse.json({received:true,event:type,paymentStatus:status,...state});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Cashfree webhook processing failed"},{status:500});}}
}
