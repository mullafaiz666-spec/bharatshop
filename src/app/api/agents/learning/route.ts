import { NextResponse } from "next/server";
import { db } from "@/db";
import { aiActivityLogs } from "@/db/schema";
import { desc, eq } from "drizzle-orm";
import { openAIJson } from "@/lib/ai/agent-tools";
export const dynamic="force-dynamic";
export async function GET(req:Request){try{const {searchParams}=new URL(req.url);const userId=Number(searchParams.get("userId")??1);const logs=await db.select().from(aiActivityLogs).where(eq(aiActivityLogs.userId,userId)).orderBy(desc(aiActivityLogs.createdAt)).limit(200);const buckets:Record<string,{events:number;profitImpactInr:number}>={};for(const l of logs){const key=l.agentName||"unknown";buckets[key]??={events:0,profitImpactInr:0};buckets[key].events+=1;buckets[key].profitImpactInr+=Number(l.profitImpactInr||0);}let analysis=null;if(logs.length&&process.env.OPENAI_API_KEY){analysis=await openAIJson("You are BharatShop Learning Agent. Analyze operational outcomes and produce only actionable, evidence-based lessons. Never invent results. Return JSON {lessons:string[],priorities:string[],risks:string[]}.",{observations:buckets,recent:logs.slice(0,50)});}return NextResponse.json({agent:"Learning-Agent",status:process.env.OPENAI_API_KEY?"ready":"blocked_missing_keys",inputs:["source price changes","delivery time","cancellations","returns/RTO","profit","advertising performance"],observations:buckets,analysis,recent:logs.slice(0,30)});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Learning analysis failed"},{status:503})}}
