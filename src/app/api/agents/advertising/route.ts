import { NextResponse } from "next/server";
import { openAIJson } from "@/lib/ai/agent-tools";
export const dynamic="force-dynamic";
type AdPlan={productId:number;productName:string;channels:string[];dailyBudgetInr:number;maxCpaInr:number;targetRoas:number;status:"draft"|"ready";rules:{pauseBelowRoas:number;scaleAboveRoas:number;maxDailyBudgetInr:number};aiStrategy?:unknown};
export async function POST(request:Request){try{const body=await request.json();const productId=Number(body.productId),productName=String(body.productName||"").trim(),sellingPrice=Number(body.sellingPriceInr),landedCost=Number(body.landedCostInr),fees=Number(body.feesInr||0),marketingAllocation=Number(body.marketingAllocationInr||0),dailyBudget=Math.max(0,Number(body.dailyBudgetInr||0));if(!Number.isInteger(productId)||!productName||!Number.isFinite(sellingPrice)||!Number.isFinite(landedCost))return NextResponse.json({error:"productId, productName, sellingPriceInr and landedCostInr are required"},{status:400});const contribution=sellingPrice-landedCost-fees;if(contribution<=0)return NextResponse.json({error:"Product is not profitable before advertising"},{status:422});const maxCpa=Math.max(1,contribution-marketingAllocation),targetRoas=Math.max(1.5,sellingPrice/maxCpa);const ai=await openAIJson("You are BharatShop Advertising Agent. Build a conservative acquisition strategy from the supplied economics. Never recommend spending that can make the product unprofitable. Return JSON {channels:string[],audience:string,creativeAngles:string[],reason:string}.",{productName,sellingPrice,landedCost,fees,contribution,maxCpa,targetRoas});const plan:AdPlan={productId,productName,channels:Array.isArray(ai.channels)?ai.channels.map(String):["google","meta"],dailyBudgetInr:Math.min(dailyBudget||Math.floor(maxCpa*2),Number(body.maxDailyBudgetInr||Math.max(maxCpa*3,300))),maxCpaInr:Math.floor(maxCpa),targetRoas:Number(targetRoas.toFixed(2)),status:"ready",rules:{pauseBelowRoas:Number(body.pauseBelowRoas||1.5),scaleAboveRoas:Number(body.scaleAboveRoas||3),maxDailyBudgetInr:Number(body.maxDailyBudgetInr||Math.max(maxCpa*3,300))},aiStrategy:ai};return NextResponse.json({plan,execution:"approval_required",message:"Strategy ready; external ad accounts must be connected before spend begins."});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Invalid request"},{status:503})}}
export async function GET(){return NextResponse.json({agent:"advertisement",status:process.env.OPENAI_API_KEY?"ready":"blocked_missing_keys",channels:["google","meta"],capabilities:["openai_strategy","budget_guard","max_cpa","roas_pause","roas_scale"],execution:"approval_required"});}
