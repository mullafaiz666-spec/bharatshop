import { NextResponse } from "next/server";
import { db } from "@/db";
import { orders, products, productImages, aiActivityLogs } from "@/db/schema";
import { eq, and, desc } from "drizzle-orm";
import { openAIJson } from "@/lib/ai/agent-tools";
export const dynamic="force-dynamic";
export async function POST(req:Request){try{const body=await req.json();const orderId=Number(body.orderId);if(!orderId)return NextResponse.json({error:"orderId required"},{status:400});const [row]=await db.select({order:orders,product:products}).from(orders).leftJoin(products,eq(orders.productId,products.id)).where(eq(orders.id,orderId)).limit(1);if(!row)return NextResponse.json({error:"Order not found"},{status:404});const o=row.order,p=row.product;if(!p)return NextResponse.json({error:"No selected source/product attached",status:"BLOCKED"},{status:409});const [sourceEvidence]=await db.select({sourceUrl:productImages.sourceUrl}).from(productImages).where(and(eq(productImages.productId,p.id),eq(productImages.status,"VERIFIED"))).orderBy(desc(productImages.createdAt)).limit(1);const sourceUrl=sourceEvidence?.sourceUrl??null;const cartPrice=Number(body.currentCartPriceInr??p.supplierCostInr),shipping=Number(body.shippingCostInr??p.shippingCostInr),stock=Number(body.stockCount??p.stockCount),deliveryDays=Number(body.deliveryDays??7),minMargin=Number(body.minMarginPct??35);const margin=Number(o.customerPaidInr)>0?(Number(o.customerPaidInr)-cartPrice-shipping)/Number(o.customerPaidInr)*100:0;const checks={stock:stock>0,cartPrice:cartPrice>0,shipping:shipping>=0,delivery:deliveryDays>0&&deliveryDays<=Number(body.maxDeliveryDays??15),margin:margin>=minMargin,sourceEvidence:Boolean(sourceUrl)};const ai=await openAIJson("You are BharatShop Order Recheck Agent. Decide whether a supplier purchase is economically and operationally safe. Never invent missing live facts. If evidence is insufficient, keep RECHECK_REQUIRED. Return JSON {decision:'PASS'|'HOLD',reason:string,risks:string[]}.",{order:{orderNumber:o.orderNumber,customerPaidInr:o.customerPaidInr},product:{title:p.title,source:p.supplierName,sourceUrl},liveEvidence:{cartPrice,shipping,stock,deliveryDays,marginPct:+margin.toFixed(2),checks}});const passed=ai.decision==="PASS"&&Object.values(checks).every(Boolean);const status=passed?"PURCHASE_PENDING":"RECHECK_REQUIRED";const decision={checkedAt:new Date().toISOString(),source:p.supplierName,sourceUrl,cartPriceInr:cartPrice,shippingInr:shipping,stock,deliveryDays,marginPct:+margin.toFixed(2),minMarginPct:minMargin,checks,ai};const [updated]=await db.update(orders).set({supplierCostInr:String(cartPrice),fulfillmentStatus:status,aiDecisionLog:`${o.aiDecisionLog}; order_time_recheck=${JSON.stringify(decision)}`}).where(eq(orders.id,orderId)).returning();await db.insert(aiActivityLogs).values({userId:updated.userId,agentName:"Order-Recheck-Agent",actionType:passed?"RECHECK_PASSED":"RECHECK_BLOCKED",message:`${updated.orderNumber}: ${passed?"AI recheck passed":"AI recheck held order"}.`,profitImpactInr:String(updated.netProfitInr),metadataJson:decision,status:passed?"SUCCESS":"BLOCKED"});return NextResponse.json({status,passed,checks,economics:decision,order:updated});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Invalid request"},{status:503})}}
export async function GET(){return NextResponse.json({agent:"Order-Recheck-Agent",status:process.env.OPENAI_API_KEY?"ready":"blocked_missing_keys",humanGate:"CEO approval before supplier purchase"})}
