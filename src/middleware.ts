import { NextRequest, NextResponse } from "next/server";
const ADMIN_PATHS=["/dashboard","/api/overview","/api/ceo-chat","/api/ceo-approvals","/api/ceo-research","/api/agent-audit","/api/agents","/api/catalog","/api/sourcing","/api/suppliers","/api/products","/api/orders","/api/rules","/api/engine","/api/shopify","/api/automation","/api/fashion-designer","/api/fashion-studio"];
async function isValidSessionCookie(value:string|undefined){if(!value)return false;const secret=process.env.ADMIN_SESSION_SECRET;if(!secret||secret.length<32)return false;const separator=value.lastIndexOf(".");if(separator<=0)return false;const token=value.slice(0,separator),signature=value.slice(separator+1),bytes=new TextEncoder().encode(`${secret}\0${token}`),digest=await crypto.subtle.digest("SHA-256",bytes),expected=Array.from(new Uint8Array(digest),b=>b.toString(16).padStart(2,"0")).join("");return signature.length===expected.length&&signature===expected;}
function isAutomationAuthorized(request:NextRequest){if(!request.nextUrl.pathname.startsWith("/api/"))return false;const expected=process.env.BHARATSHOP_AUTOMATION_TOKEN||process.env.AUTOMATION_TOKEN;if(!expected)return false;const supplied=request.headers.get("authorization")?.replace(/^Bearer\s+/i,"")||request.headers.get("x-automation-token")||"";return supplied===expected;}
export async function middleware(request:NextRequest){const{pathname}=request.nextUrl,protectedPath=ADMIN_PATHS.some(p=>pathname===p||pathname.startsWith(`${p}/`));if(!protectedPath)return NextResponse.next();if(isAutomationAuthorized(request))return NextResponse.next();const valid=await isValidSessionCookie(request.cookies.get("bharatshop_admin_session")?.value);if(!valid){if(pathname.startsWith("/api/"))return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.redirect(new URL("/admin-login",request.url));}return NextResponse.next();}
export const config={matcher:["/dashboard/:path*","/api/:path*"]};
