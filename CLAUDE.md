# Claude Code Instructions for BharatShop

Follow the repository-wide rules in `AGENTS.md`.

For this project, Claude Code is a delegated implementation/review subagent under DeepSeek Harness. Work only inside the current BharatShop workspace, preserve the existing application architecture, and do not inspect credential files or mutate production data.

Prefer scoped implementation, refactoring, debugging, and code review tasks. Run relevant checks before reporting success. Never bypass authentication, payment protections, approval gates, database safety checks, or Git history protections to make a task appear complete.
